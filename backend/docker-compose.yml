services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: pulse
      POSTGRES_USER: pulse
      POSTGRES_PASSWORD: pulse-local-only
    ports:
      - "5432:5432"
    volumes:
      - pulse_postgres:/var/lib/postgresql/data

volumes:
  pulse_postgres:
